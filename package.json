{
  "name": "varenikwalksdog",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gh-pages:deploy": "gh-pages -d dist",
    "deploy": "npm run build && npm run gh-pages:deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.13"
  }
}
